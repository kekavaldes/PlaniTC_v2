import streamlit as st


def render_reconstruccion():
    st.subheader("Reconstrucción")
    st.caption("Base para replicar la estructura por adquisición y agregar una o más reconstrucciones por exploración.")
    st.info("Aquí debes migrar la pestaña de reconstrucción desde tu archivo original.")
